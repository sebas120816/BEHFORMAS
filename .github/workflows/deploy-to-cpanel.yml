name: Deploy to behformas.com (FTP)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Deploy public_html via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.4.0
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./public_html/
          server-dir: ./public_html/
          state-name: .ftp-deploy-sync-state-public_html.json
          dangerous-clean-slate: false
          exclude: |
            **/.git*
            **/.DS_Store
            **/maintenance-check.php
            **/.env
            **/.env.local

      - name: Deploy data directory via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.4.0
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./data/
          server-dir: ./data/
          state-name: .ftp-deploy-sync-state-data.json
          dangerous-clean-slate: false

      - name: Deployment Summary
        run: |
          echo "✅ Deployment completed!"
          echo "📍 Files deployed to: behformas.com/public_html"
          echo "📍 Data directory updated"
          echo ""
          echo "Next steps:"
          echo "1. Verify at: https://behformas.com/maintenance.html"
          echo "2. Configure openai_config.php manually on server"
          echo "3. Test API endpoints"